import { ThreeCharacterPipe } from './three-character.pipe';

describe('ThreeCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new ThreeCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
